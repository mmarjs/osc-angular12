import { SumArrayValuesPipe } from './sum-array-values.pipe';

describe('SumArrayValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new SumArrayValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
